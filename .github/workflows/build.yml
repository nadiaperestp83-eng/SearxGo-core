name: SearxGo Android Build

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Install Python 3.11 e dependências
        run: |
          sudo apt-get update
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository -y ppa:deadsnakes/ppa
          sudo apt-get update
          sudo apt-get install -y python3.11 python3.11-distutils python3.11-venv build-essential git curl lsb-release openjdk-17-jdk lib32stdc++6 lib32z1 pkg-config unzip
          sudo update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.11 1
          sudo update-alternatives --set python3 /usr/bin/python3.11

      - name: Forçar Instalação do CIPD e depot_tools
        run: |
          # 1. Baixa o binário oficial do CIPD do Google
          curl -sSf https://chrome-infra-packages.appspot.com/client?os=linux-amd64&arch=amd64 | tar -xz -C /usr/local/bin
          # 2. Clona depot_tools
          git clone --depth=1 https://chromium.googlesource.com/chromium/tools/depot_tools.git ${{ github.workspace }}/depot_tools
          # 3. Adiciona ao PATH
          echo "${{ github.workspace }}/depot_tools" >> $GITHUB_PATH
          export PATH="${{ github.workspace }}/depot_tools:$PATH"
          # 4. Inicializa
          cd ${{ github.workspace }}/depot_tools
          ./gclient

      - name: Configurar gclient
        run: |
          export PATH="${{ github.workspace }}/depot_tools:$PATH"
          cd ${{ github.workspace }}
          gclient config --name src https://github.com/brave/brave-browser --unmanaged

      - name: gclient sync
        run: |
          export PATH="${{ github.workspace }}/depot_tools:$PATH"
          cd ${{ github.workspace }}
          gclient sync --no-history --shallow --delete_unversioned_trees --reset --nohooks --jobs=8

      - name: gclient runhooks
        run: |
          export PATH="${{ github.workspace }}/depot_tools:$PATH"
          cd ${{ github.workspace }}
          gclient runhooks

      - name: Build
        run: |
          export PATH="${{ github.workspace }}/depot_tools:$PATH"
          cd ${{ github.workspace }}/src
          gn gen out/arm64
          autoninja -C out/arm64 chrome_public_apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: searxgo-arm64
          path: src/out/arm64/apks/ChromePublic.apk
          retention-days: 7
