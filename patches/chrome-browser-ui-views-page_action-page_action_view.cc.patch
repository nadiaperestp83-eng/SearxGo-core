diff --git a/chrome/browser/ui/views/page_action/page_action_view.cc b/chrome/browser/ui/views/page_action/page_action_view.cc
index 5f46fdaea39c77df5f714660648025bb1eeb1eca..83cad3d58e2fa22997e8735bba42143315f411e9 100644
--- a/chrome/browser/ui/views/page_action/page_action_view.cc
+++ b/chrome/browser/ui/views/page_action/page_action_view.cc
@@ -173,7 +173,7 @@ void PageActionView::OnPageActionModelChanged(
   if (!visible) {
     ResetSlideAnimation(/*show=*/false);
     NotifyIsChipShowingChange();
-  } else if (model.ShouldShowSuggestionChip()) {
+  } else if (model.ShouldShowSuggestionChip() || model.GetAlwaysShowLabel()) {
     if (model.GetShouldAnimateChipIn()) {
       AnimateIn(/*string_id=*/std::nullopt);
     } else {
@@ -258,6 +258,7 @@ void PageActionView::UpdateBorder() {
       !observation_.GetSource()->GetImage().IsVectorIcon()) {
     border_insets = GetInsetsForNonVectorIcon();
   }
+  MaybeOverrideBorder(observation_.GetSource(), border_insets);
   SetBorder(views::CreateEmptyBorder(border_insets));
 }
 
@@ -304,6 +305,8 @@ void PageActionView::NotifyClick(const ui::Event& event) {
               kPageActionEntryPointKey,
               static_cast<std::underlying_type_t<PageActionEntryPoint>>(
                   PageActionEntryPoint::kSuggestionChip))
+          .SetProperty(kBravePageActionEventFlagKey,
+                       event.IsMouseEvent() ? event.flags() : ui::EF_NONE)
           .Build());
 }
 
